-- 열의 추가/수정

--추가 add
--90번 부서직원들 테이블 EMP90
CREATE TABLE EMP90
AS
SELECT employee_id,first_name,last_name,salary,department_id
FROM employees
WHERE department_id = 90;

SELECT*FROM EMP90;

DESC EMP90; 

ALTER TABLE EMP90
ADD (GENDER VARCHAR2(1));

ALTER TABLE EMP90
ADD(COM1 NUMBER DEFAULT 0 NOT NULL);

ALTER TABLE EMP90
ADD(CREATED_DATE DATE DEFAULT SYSDATE);

DESC EMP90;

--GENDER 열의 크기 1 -> 100으로 수정
ALTER TABLE EMP90
MODIFY(GENDER VARCHAR2(100));

--열의 삭제
ALTER TABLE EMP90
DROP COLUMN CREATED_DATE;

ALTER TABLE EMP90
DROP COLUMN COM1;

ALTER TABLE EMP90
DROP COLUMN GENDER;

DESC EMP90;


--이름수정
--열의 이름수정
ALTER TABLE EMP90
RENAME COLUMN DEPARTMENT_ID TO 부서번호;

RENAME EMP90 TO 부서90;

DESC 부서90;


















